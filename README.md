# AI Sana Challenge Hub — от бизнес-задачи к решению

## Краткое описание

Бизнес описывает задачу для студенческой команды слишком коротко, и студентам трудно понять, за что браться. Проект — MVP-платформа, которая помогает представителю бизнеса превратить черновик потребности в полноценную карточку задачи, оценивает качество её заполнения прозрачным рейтингом и публикует в открытом каталоге, где студенческие команды сами откликаются, а бизнес сам выбирает, с кем работать.

Для кого: представители бизнеса (заказчики задач) и студенческие команды хакатона/трека «Образование».

## Что реализовано

- Ввод черновика бизнес-задачи
- ИИ-анализ черновика и генерация ≥3 уточняющих вопросов по недостающим категориям
- Сборка структурированной карточки задачи из черновика и ответов (без домысливания фактов)
- Детерминированная формула рейтинга (0–100) по 7 взвешенным категориям с расшифровкой баллов
- 4 уровня готовности задачи (черновик / рабочая / готовая / приоритетная)
- Публикация подтверждённой задачи в общий каталог, отсортированный по рейтингу
- Отклик студенческой команды на задачу (идея, план, ссылка на прототип) — из 5 seed-профилей команд
- Ручной выбор бизнеса: принять или отклонить отклик, без автоназначения
- _В разработке: фильтры каталога по теме/готовности, тестовые синтетические данные (seed), деплой на Vercel_

## Как работает решение

Сквозной сценарий:

1. **Черновик** — представитель бизнеса вводит краткое описание потребности.
2. **Уточнение** — ИИ определяет недостающие сведения из 7 категорий и задаёт ≥3 уместных вопроса.
3. **Карточка** — из черновика и ответов собирается редактируемая карточка задачи, представитель бизнеса подтверждает.
4. **Рейтинг** — система начисляет баллы (0–100) за полноту карточки по прозрачной формуле, без ИИ, и показывает, что повысит рейтинг.
5. **Каталог** — подтверждённая задача публикуется на позиции, соответствующей рейтингу.
6. **Отклик** — любая студенческая команда просматривает каталог и подаёт предложение (идея, план, срок, ссылка).
7. **Выбор** — бизнес сравнивает отклики и вручную выбирает одну, несколько или ни одной команды; автоматическое назначение запрещено.

## Технологии

- Next.js 16 (App Router), React 19, TypeScript
- Tailwind CSS 4
- Claude API (`@anthropic-ai/sdk`, модель `claude-opus-5`) — единственный внешний AI-сервис
- Деплой: Vercel

## Архитектура проекта

```
app/
  page.tsx              — экран ввода черновика и уточняющих вопросов
  api/clarify/route.ts  — вызов Claude: анализ черновика → JSON с вопросами
  api/card/route.ts     — вызов Claude: черновик + ответы → структурированная карточка
lib/
  types.ts              — модели Task, TaskCard, Team, TeamResponse
  rating.ts             — детерминированная формула рейтинга (без ИИ)
  prompts.ts            — системные промпты для обеих AI-функций
```

Поток данных: `page.tsx` (single-page state machine: draft → questions → card → published) → `POST /api/clarify` → Claude (`CLARIFY_PROMPT`) → вопросы → пользователь отвечает → `POST /api/card` → Claude (`CARD_PROMPT`) → структурированная карточка → рейтинг считается **локально** в `lib/rating.ts`, без ИИ → публикация в каталог (React state) → отклик команды (seed-профили) → ручной выбор бизнеса (accept/decline).

## Установка и запуск

```bash
git clone https://github.com/BAITC-Hacks/hack-422c070c-orta.git
cd hack-422c070c-orta
npm install
cp .env.example .env.local
# вписать ключ: ANTHROPIC_API_KEY=sk-ant-...
npm run dev
```

Открыть http://localhost:3000

## Как проверить решение

1. Открыть главную страницу.
2. Ввести слабый черновик, например: *"Нужно автоматизировать проверку заявок клиентов"*.
3. Нажать «Проанализировать черновик».
4. Получить ≥3 уточняющих вопроса по категориям, которые не раскрыты в черновике (например, данные, критерии успеха, связь с бизнесом).

Если `ANTHROPIC_API_KEY` не задан или неверен — эндпоинт вернёт явную ошибку авторизации, а не тихий сбой (см. обработку в `app/api/clarify/route.ts`).

## Данные и интеграции

- Единственная внешняя интеграция — Claude API (Anthropic).
- `lib/seed.ts` — 5 черновиков бизнес-задач разного качества (включая слабый — для демонстрации роста рейтинга) и 5 профилей студенческих команд (название, интересы, навыки, технологии), соответствует минимальному объёму из ТЗ.
- Черновики доступны в UI как кнопки быстрого выбора ("Пример 1"–"Пример 5") на первом шаге.

## Ограничения

- Нет персистентного хранилища (БД) — каталог и отклики живут в состоянии React на клиенте (в рамках сессии), что соответствует ограничению объёма из ТЗ (полноценная инфраструктура не требуется).
- Нет авторизации/ролевой модели — намеренно, согласно ограничению объёма кейса.
- ИИ используется только для анализа черновика и сборки карточки — не участвует в расчёте рейтинга и не выбирает команду за бизнес (выбор — только ручная кнопка представителя бизнеса).
- Тема задачи (`topic`) определяется ИИ на этапе сборки карточки, отдельного справочника тем нет — фильтр строится по фактически встретившимся темам.

## Ссылка на deployed-версию

_Будет добавлена после деплоя на Vercel._

---

## Соответствие критериям оценки (100 баллов)

| Критерий | Баллы | Статус |
|---|---|---|
| Сквозной сценарий | 20 | ✅ весь флоу работает: черновик → вопросы → карточка → рейтинг → каталог → отклик → выбор |
| Качество карточки | 15 | ✅ вопросы уместны, карточка редактируется вручную перед публикацией |
| Геймификация бизнеса | 25 | ✅ формула рейтинга готова, пересчёт на лету (`lib/rating.ts`) |
| Каталог и отклики | 15 | ✅ каталог с сортировкой по рейтингу, фильтрами по теме и готовности, отклики, ручной accept/decline |
| AI-функция | 10 | ✅ обе функции реализованы, без домысливания фактов |
| Техническое качество | 10 | ✅ `npm run build` проходит без ошибок, обработка ошибок API |
| Демонстрация | 5 | 🟡 репетиция примера — следующий шаг |

## Журнал разработки (коммиты каждый час)

| Время | Что сделано |
|---|---|
| 09:55 | Инициализация нового репозитория-черновика, README-заготовка |
| ~12:00–13:00 | Каркас Next.js, модели данных, формула рейтинга, промпты, API уточняющих вопросов, страница ввода черновика |
| 13:4x | Перенос проекта в официальный репозиторий команды (`hack-422c070c-orta`), README по шаблону организаторов |
| 14:0x | Полный сквозной сценарий: сборка карточки (`/api/card`), редактирование карточки с live-рейтингом, каталог, отклики команд (5 seed-профилей), ручной выбор бизнеса |
| 14:2x | `lib/seed.ts`: 5 тестовых черновиков (включая слабый — для демо) и 5 профилей команд, кнопки быстрого выбора примера в UI |
| 14:4x | Поле "тема" в карточке (заполняется ИИ при сборке), фильтры каталога по теме и уровню готовности — закрывает требование ТЗ |

## Команда

- Oscar Altynbekov (капитан)
